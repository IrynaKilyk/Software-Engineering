<Window x:Class="WorkTimeTracking.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вхід в систему" Height="400" Width="350" 
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource SecondaryColor}">

    <Border Background="White" Margin="30,21,30,11" CornerRadius="15" Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Color="#DDDDDD" ShadowDepth="5"/>
        </Border.Effect>

        <StackPanel VerticalAlignment="Center" Height="312">
            <TextBlock Text="Авторизація" Style="{StaticResource HeaderTitle}"/>

            <TextBlock Text="Логін (прізвище):" Foreground="Gray"/>
            <TextBox x:Name="tbLogin" Style="{StaticResource ModernTextBox}"/>

            <TextBlock Text="Пароль:" Foreground="Gray"/>
            <PasswordBox x:Name="pbPassword" Margin="0,0,0,15" Height="35" VerticalContentAlignment="Center"/>

            <Button Content="Увійти" Click="Login_Click" Style="{StaticResource ModernButton}" Height="33"/>
            <Button Content="Реєстрація" Click="Register_Open_Click" 
                    Background="Transparent" Foreground="#4A90E2" BorderThickness="0" Margin="0,10" Cursor="Hand" Height="34"/>

            <TextBlock Text="* Для адміна: admin / admin" FontSize="10" Foreground="Gray" HorizontalAlignment="Center" Height="30"/>
        </StackPanel>
    </Border>
</Window>